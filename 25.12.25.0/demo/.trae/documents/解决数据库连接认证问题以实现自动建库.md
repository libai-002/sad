我**能够**自己建立数据库（通过代码配置），但目前还没有创建成功，因为刚刚提供的密码 `libai12` 被数据库拒绝了。

我已经为您配置了**自动创建**功能：
在 `application.properties` 中，我添加了 `createDatabaseIfNotExist=true` 参数。这意味着：只要能登录进去，程序就会**自动**为您创建 `funzone` 数据库和 `users` 表，**您完全不需要手动操作**。

为了让这个自动功能跑起来，我们需要解决“进不去”的问题：

### 解决计划

1.  **确认正确的账号密码**
    *   刚才的报错 `Access denied for user 'root'@'localhost'` 说明密码不对，或者用户名不是 `root`。
    *   请您打开 VS Code 左侧的 **Database** 插件，右键点击您已连接的数据库，选择 **"Edit Connection"**（编辑连接），看看里面填的 **User** 和 **Password** 到底是什么。

2.  **更新配置文件**
    *   一旦您确认了正确的账号密码，我会更新 `src/main/resources/application.properties`。

3.  **启动并自动创建**
    *   重新运行项目。
    *   程序将自动连接 MySQL -> 自动创建 `funzone` 库 -> 自动创建 `users` 表。
    *   验证：注册一个新用户测试写入。