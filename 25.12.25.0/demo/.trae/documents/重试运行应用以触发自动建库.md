好的，既然您希望重试，我将再次运行 Spring Boot 应用。如果您的数据库环境（如权限或密码）在后台已经调整好，这次启动将会自动完成数据库的创建。

### 执行计划

1.  **检查配置**：确认 `application.properties` 中仍配置了 `createDatabaseIfNotExist=true` 且密码为 `libai12`。
2.  **启动应用**：运行 `mvn spring-boot:run`。
    *   **预期行为**：Spring Boot 尝试连接 MySQL。
    *   **关键动作**：如果连接成功，JDBC 驱动会自动创建 `funzone` 数据库（如果不存在）。
    *   **后续动作**：Hibernate 会自动创建 `users` 表。
3.  **结果验证**：
    *   观察控制台日志，确认是否还有 `Access denied` 错误。
    *   如果启动成功，说明数据库创建完成。

我准备好再次启动了。